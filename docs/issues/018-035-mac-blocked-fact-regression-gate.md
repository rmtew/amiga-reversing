# 018-035: Mac Blocked Fact Regression Gate

Status: completed

## Proposal Context

- Source proposal: `docs/proposals/018-platform-executable-format-knowledge.md`
- Blocked by: `docs/issues/018-030-executable-kb-restart-and-state-sync.md`
- Stronger after: `docs/issues/018-034-parser-fact-coverage-report.md` and
  `docs/issues/018-037-macos-blocker-resolution-or-final-deferral.md`
- Purpose: ensure Mac byte-entry and relocation/fixup facts remain blocked or
  deferred until real accepted evidence exists.

## Knowledge Delta

No new accepted Mac facts are expected. This issue should strengthen the
negative evidence boundary:

- `movea.l (a7)+,a0` may remain candidate evidence only;
- nonzero CODE byte-entry rules must not be accepted without citation or
  parser assertion;
- classic 68K CODE relocation/fixup parsing must remain deferred without exact
  record location, encoding, affected offsets, and application rules;
- web/artifact/listing output must label these states as candidate/deferred.

## Default Behavior

Current Mac views should remain available. The gate must prevent status
promotion, not remove candidate visibility.

## Evidence Standard

Tests must prove consumers cannot treat candidate byte-entry or deferred
relocation/fixup state as accepted parser authority.

Completed gate:

- Mac C backend summary output is covered by parser fact coverage tests and
  contains accepted, candidate, and deferred refs with zero invalid claims.
- Mac project payload output is coverage-checked; all
  `macos.code_resource.movea_stack_a0.boundary.candidate` refs classify as
  candidate, and all `macos.segment_loader.relocation_fixups.deferred` refs
  classify as deferred.
- The committed Mac target artifact test now rejects accepted parser-use text on
  movea byte-entry and relocation/fixup lines.
- The web payload fixture now rejects any candidate/deferred fact status paired
  with `accepted_parser_output`.

## Implementation Slice

AFK slice:

- audit current Mac parser/listing/project/artifact/web paths for byte-entry and
  relocation status text/fact ids;
- add regression tests for blocked/deferred state at the strongest current
  surfaces;
- update validator/report output if needed so failures are obvious;
- update Proposal 018 and/or Proposal 012 with any remaining gap found.

## Research Completion Standard

Complete only after a first-pass path audit and a second-pass negative-safety
review. Do not close by claiming accepted evidence; close by proving the block
is enforced.

## Research Coverage

- [x] Mac C parser summary output checked.
- [x] Python Mac listing/project payload checked.
- [x] Committed Mac target artifact checked.
- [x] Web payload/rendering checked where currently covered by tests.
- [x] Second-pass review checked for accepted-status leakage.

## Research Review

- [x] Byte-entry remains candidate/deferred everywhere.
- [x] Relocation/fixup remains deferred everywhere.
- [x] Candidate visibility remains available to users.
- [x] No Mac source/artifact churn was committed unless generated by an
  intentional behavior-preserving status correction.

## Required Sign-Off

- [x] Targeted Mac parser/listing/artifact tests pass.
- [x] Platform executable format tests pass.
- [x] Any web-visible status change has matching test coverage.

## Completion Evidence

- `uv run python -m pytest tests/test_macos_c_backend.py -q` passed.
- `uv run python -m pytest tests/test_macos_project_payload.py
  tests/test_macos_target_artifact.py
  tests/test_web_e2e_cdp.py::test_macos_preview_payload_keeps_blocked_facts_non_accepted
  -q` passed.
- `uv run python -m pytest tests/test_platform_executable_formats.py -q`
  passed.
- `uv run ruff check tests/test_macos_project_payload.py
  tests/test_macos_target_artifact.py tests/test_web_e2e_cdp.py` passed.
